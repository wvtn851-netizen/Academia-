<!DOCTYPE html> <html lang="pt-BR"> <head>   <meta charset="UTF-8">   <meta name="viewport" content="width=device-width, initial-scale=1.0">   <title>FORGE ACADEMIA</title>   <script src="https://cdn.tailwindcss.com"></script>   <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css">   <style>     body {       background: #0f0f0f;       color: #e5e5e5;       font-family: 'Inter', system-ui, sans-serif;     }     .card {       background: #1a1a1a;       border: 1px solid #3a3a3a;       border-radius: 20px;     }     .gold-text {       color: #facc15;     }     .red-accent {       color: #ef4444;     }     .btn-primary {       background: #ef4444;       color: white;       transition: all 0.3s;     }     .btn-primary:hover {       background: #dc2626;       transform: translateY(-2px);     }     .input-field {       background: #111111;       border: 2px solid #3a3a3a;       border-radius: 12px;       color: white;     }     .input-field:focus {       border-color: #facc15;       box-shadow: 0 0 0 3px rgba(250, 204, 21, 0.2);     }   </style> </head> <body class="min-h-screen pb-12">    <div class="max-w-2xl mx-auto p-4">      <!-- Header -->     <div class="text-center mb-8 mt-8">       <h1 class="text-4xl font-bold flex items-center justify-center gap-3">         <i class="fas fa-dumbbell gold-text"></i>          FORGE ACADEMIA       </h1>       <p class="text-gray-400 mt-2"treino dos guri</p>     </div>      <!-- Navegação -->     <div class="flex justify-center gap-2 mb-8 bg-zinc-900 p-1.5 rounded-2xl w-fit mx-auto">       <button onclick="showTab(0)" class="tab-btn active px-6 py-3 rounded-xl font-medium flex items-center gap-2 text-white" id="tab0">         <i class="fas fa-plus"></i> Registrar       </button>       <button onclick="showTab(1)" class="tab-btn px-6 py-3 rounded-xl font-medium flex items-center gap-2 text-white" id="tab1">         <i class="fas fa-chart-line"></i> Progresso       </button>       <button onclick="showTab(2)" class="tab-btn px-6 py-3 rounded-xl font-medium flex items-center gap-2 text-white" id="tab2">         <i class="fas fa-history"></i> Histórico       </button>     </div>      <!-- Tab Registrar -->     <div id="tab-content-0" class="tab-content">       <div class="card p-8">         <h2 class="text-2xl font-semibold mb-6 text-center gold-text">REGISTRAR TREINO</h2>                  <input type="date" id="data" class="input-field w-full p-4 mb-4 text-lg">                  <input type="text" id="exercicio" placeholder="Exercício (ex: Supino Reto, Agachamento)"                 class="input-field w-full p-4 mb-4 text-lg">                  <div class="grid grid-cols-2 gap-4">           <div>             <label class="gold-text text-sm font-medium">PESO (kg)</label>             <input type="number" id="peso" placeholder="80"                     class="input-field w-full p-4 text-2xl">           </div>           <div>             <label class="gold-text text-sm font-medium">REPETIÇÕES</label>             <input type="number" id="reps" placeholder="10"                     class="input-field w-full p-4 text-2xl">           </div>         </div>          <button onclick="registrar()"                  class="btn-primary w-full py-5 text-xl font-semibold rounded-2xl mt-8 flex items-center justify-center gap-3">           <i class="fas fa-save"></i> SALVAR TREINO         </button>       </div>     </div>      <!-- Tab Progresso -->     <div id="tab-content-1" class="tab-content hidden">       <div class="card p-8">         <h2 class="text-3xl font-semibold text-center mb-8 gold-text">PROGRESSO SEMANAL</h2>         <div id="resumo" class="text-center"></div>       </div>     </div>      <!-- Tab Histórico -->     <div id="tab-content-2" class="tab-content hidden">       <div class="card p-6">         <h2 class="text-2xl font-semibold mb-6 text-center gold-text">HISTÓRICO DE TREINOS</h2>         <div id="historico" class="space-y-4"></div>       </div>     </div>    </div>    <script>     let treinos = JSON.parse(localStorage.getItem('treinosForge')) || [];      function showTab(tab) {       document.querySelectorAll('.tab-content').forEach(el => el.classList.add('hidden'));       document.getElementById(`tab-content-${tab}`).classList.remove('hidden');              document.querySelectorAll('.tab-btn').forEach(el => el.classList.remove('active', 'bg-red-600', 'text-white'));       document.getElementById(`tab${tab}`).classList.add('active', 'bg-red-600', 'text-white');              if (tab === 1) mostrarProgresso();       if (tab === 2) mostrarHistorico();     }      function registrar() {       const data = document.getElementById('data').value;       const exercicio = document.getElementById('exercicio').value.trim();       const peso = parseFloat(document.getElementById('peso').value);       const reps = parseInt(document.getElementById('reps').value);        if (!data || !exercicio || !peso || !reps) {         alert("Preencha todos os campos.");         return;       }        treinos.push({ data, exercicio, peso, reps });       localStorage.setItem('treinosForge', JSON.stringify(treinos));        alert("Treino registrado com sucesso!");       limparCampos();       mostrarHistorico();       mostrarProgresso();     }      function limparCampos() {       document.getElementById('exercicio').value = '';       document.getElementById('peso').value = '';       document.getElementById('reps').value = '';     }      function mostrarHistorico() {       const div = document.getElementById('historico');       div.innerHTML = '';        treinos.sort((a,b) => new Date(b.data) - new Date(a.data));        treinos.forEach((t, index) => {         div.innerHTML += `           <div class="border border-zinc-700 bg-zinc-950 rounded-2xl p-6 hover:border-red-500 transition-all">             <div class="flex justify-between items-start">               <div>                 <p class="text-red-500 font-medium">${t.data}</p>                 <p class="text-xl font-semibold mt-1">${t.exercicio}</p>                 <p class="text-3xl font-bold gold-text mt-3">${t.peso} kg × ${t.reps}</p>               </div>               <button onclick="deletar(${index})" class="text-red-500 hover:text-red-600 text-2xl">                 <i class="fas fa-trash"></i>               </button>             </div>           </div>`;       });        if (treinos.length === 0) {         div.innerHTML = `<p class="text-center text-gray-500 py-16">Nenhum treino registrado ainda.</p>`;       }     }      function deletar(index) {       if (confirm("Apagar este treino?")) {         treinos.splice(index, 1);         localStorage.setItem('treinosForge', JSON.stringify(treinos));         mostrarHistorico();         mostrarProgresso();       }     }      function mostrarProgresso() {       const div = document.getElementById('resumo');              if (treinos.length === 0) {         div.innerHTML = `<p class="text-gray-400">Registre treinos para acompanhar seu progresso.</p>`;         return;       }        const 
